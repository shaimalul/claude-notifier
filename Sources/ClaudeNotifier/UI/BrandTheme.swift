import SwiftUI

extension Color {
    /// Icon palette — matches the app icon exactly
    static let iconBackground = Color(red: 0.07, green: 0.07, blue: 0.10)

    /// Near-white foreground used on dark icon backgrounds
    static let iconForeground = Color(red: 0.97, green: 0.97, blue: 1.00)

    /// Accent — used only for CTA actions (Enable button, version badge)
    static let brandPrimary = Color(red: 0.98, green: 0.47, blue: 0.14)
}
